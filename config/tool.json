{
  "type": "function",
  "name": "summarize_sales",
  "description": "Leer ventas y costos directos y calcular el resumen comercial.",
  "strict": true,
  "parameters": {
    "type": "object",
    "properties": {
      "scenario": {
        "type": "string",
        "enum": [
          "01",
          "02",
          "03"
        ]
      }
    },
    "required": [
      "scenario"
    ],
    "additionalProperties": false
  }
}
